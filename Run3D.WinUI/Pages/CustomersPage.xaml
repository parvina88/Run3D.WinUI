<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Run3D.WinUI.Pages.CustomersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Run3D.WinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <controls:DataGrid x:Name="CustomersDataGrid"
                           AutoGenerateColumns="False"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Margin="10">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Customer ID" Binding="{Binding CustomerID}" Width="*"/>
                <controls:DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <controls:DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <controls:DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </StackPanel>
</Page>
